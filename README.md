# word-search-solver

## Install dependencies

  sudo apt install libasound2-dev libudev-dev pkg-config
  
## Run the game

  cargo run --release
  
## Run tests for library

  cargo test --release
  
## Change config 
